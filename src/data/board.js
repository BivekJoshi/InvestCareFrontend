/**
 * Board of Directors, served from the CMS.
 * See the note in `company.js` — edit this content in the CMS, not here.
 */

import site from './generated/site.json';

export const board = site.board;
