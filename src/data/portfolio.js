/**
 * Portfolio holdings, served from the CMS.
 * See the note in `company.js` — edit this content in the CMS, not here.
 */

import site from './generated/site.json';

export const portfolio = site.portfolio;

export const portfolioStats = site.portfolioStats;

/** Open roles. Empty until a job is published in the CMS. */
export const jobs = site.jobs;
